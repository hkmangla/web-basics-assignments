import { SpaceValidatorDirective } from './space-validator.directive';

describe('SpaceValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SpaceValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
